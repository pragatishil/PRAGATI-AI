# Models

Place trained model artifacts here after the complete v10 collection has been consolidated.

Suggested distinction:
- `checkpoints/final/` — checkpoints needed for the reported manuscript results
- `checkpoints/ablations/` — checkpoints supporting reported ablation experiments
- `checkpoints/archive/` — intermediate/temporary checkpoints retained for provenance
