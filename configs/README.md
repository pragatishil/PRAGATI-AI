# Configurations

The supplied notebook currently contains hard-coded Kaggle input/output paths.

After consolidation, move dataset locations, output locations, seeds and major experiment settings into explicit configuration files here. This should be done as a controlled refactor after the current v10 artifact set has been preserved.
